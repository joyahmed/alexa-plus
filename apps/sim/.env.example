MCP_URL=http://127.0.0.1:3029/mcp
MCP_TOKEN=            # same value as apps/mcp .env
GEMINI_API_KEY=       # optional; without it AGENT=scripted
GEMINI_MODEL=gemini-2.5-flash
AGENT=                # gemini | scripted (default: gemini when a key exists)
