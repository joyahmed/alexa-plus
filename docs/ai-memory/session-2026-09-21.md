# Session 2026-09-21 — three houses, then the Devpost submit

**Landed (pushed, CI deploy green, live):**
- `7b72cf7` three houses on one server: `apps/mcp/src/db/seed.ts` is data-driven (`PROPERTIES`),
  `lakeview` / `harbor` / `pineridge`. `/mcp?property=<id>` picks the unit (404 for unknown),
  `/health` lists them. Sim: `?house=<id>` on the page → `AgentRequest.house` → `connectHouse(house)`.
  Scripted agent picks the supply item from the house's own `check_supplies` list (loft has beans,
  not pods). Tests 18 (mcp: `test/portfolio.test.ts` new) + 4 (sim) = 22. Both builds green.
- `0976cd5` submission sheet updated (three houses, test count, testing step 5).

**Seed shape (relative to today):** lakeview Priya −1..+3, Okafors +4..+9 · harbor Marcus −3..+1,
Elena and Tom +2..+6 (repair today → skips +1, +2 → lands +3) · pineridge Lindqvists 0..+5 (checked
in today), Aisha +7..+10, Jonah −6..−2; no electrician → `no_vendor` branch is real.

**Joy, mid-session:** *"we needa submit the project"*, *"you have to use chrome and submit. video
we'd do later"*. This session has no browser tools (Claude in Chrome not attached), so the
submit is queued for a session started with the Chrome bridge (`/chrome`, or `claude --chrome`).

**Next — the Devpost submit, in a Chrome-enabled session:**
1. https://amazonappdev2026.devpost.com/ → Joy is logged in as joythegreatone@gmail.com.
2. Fill every field from `docs/devpost-submission.md` (name, tagline, About, Built with, links,
   testing instructions, feedback = paste `docs/friction-log.md`). Track: Alexa+.
3. Gallery: fresh screenshots of the live site — home at rest, hot-tub card, plumber booking with
   the MCP drawer open, `?house=harbor` radiator booking, phone width. `docs/proof/*.png` are stale.
4. Video field: leave empty. Joy adds voice later; Devpost is editable until Oct 23 12:00 PDT.
5. Stop at the final **Submit** button — Joy clicks it.

**Then the Phase 2 leftovers (after submit, entry stays editable):** proactive check-in/checkout
cards + "plumber at 3 for the shower you reported yesterday" on the home screen (also fixes
`Ambient.tsx`, which hardcodes "Lakeview Cabin · checkout Tue 11 am" — wrong for `?house=`);
MCP App `property-card`; README architecture diagram; tag `phase-2`.

**Gate:** `source ~/.nvm/nvm.sh && nvm use 24` first — the default shell resolves the Windows
pnpm under Node 20 and dies on `?.`. Then `pnpm typecheck && pnpm lint && pnpm test`.
