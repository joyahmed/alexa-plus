# Session 2026-09-20 — design day + Phase 1 skeleton

**Landed (all pushed to `joyahmed/alexa-plus` main):**
- `84c62cc` DESIGN.md — idea chosen against the judges' creative list: **the house that explains
  itself** (Alexa+ as the resident agent of a short-term rental). Runner-up: medication/caregiving.
- `54c1d76` PLAN.md rewritten for it; retired ideas A/B/C.
- `adbe36b` verification baseline (docs-only at the time; re-measured below).
- `105b1bf` **Phase 1 MCP skeleton**: `apps/mcp` — SDK 1.30.0 (protocol 2025-11-25), Hono,
  `WebStandardStreamableHTTPServerTransport` stateless + JSON responses, bearer auth, `node:sqlite`
  seed (Lakeview Cabin), tools `get_property_guide` / `get_current_stay` / `report_issue`,
  6 vitest e2e tests green, Inspector CLI proof in `docs/proof/phase-1-inspector.md`.

**Assumed, not confirmed (override any time):** idea = #1; repo/folder name stays `alexa-plus`;
endpoint `alexa.zettabyteincorp.com/mcp`; live sim on Gemini free tier, recording on `claude -p`.

**Gate:** `pnpm typecheck && pnpm lint && pnpm test` — 6/6. Build + `dist/server.js` `/health` OK.

**Gotchas found:** (1) a `.sql` beside `src` is not copied by `tsc` → schema is a TS string.
(2) `pkill -f '<pattern>'` from the assistant's shell kills the shell itself when the command
line contains the pattern — use `pkill -f 'node dist/server[.]js'`. (3) SSE responses + closing the
transport in `finally` hangs the client; stateless = `enableJsonResponse: true`, no manual close.

- `1aa5b57` deploy: `.github/workflows/deploy.yml` + `ecosystem.config.cjs`, port 3029. The
  server-side first run was refused by the auto-mode classifier ("Production Deploy") — exact
  commands for Joy in `docs/ai-memory/deploy.md`.

- Phase 2 tools: `apps/mcp/src/tools/house.ts` + `supplier.ts` (interface + simulated impl).
  `schedule_repair` skips changeover days (any check-in/check-out), urgent = today. 13/13 tests.
  Proof: `docs/proof/phase-2-inspector.md`.

- `ba1415d` `apps/sim`: the simulated Alexa+ (see PLAN Phase 2). Verified: `pnpm typecheck/lint/test`
  (13 + 3 tests), `NEXT_DIST_DIR=.next-verify pnpm build` (standalone), then MCP on 3101 + sim on
  3102 booted from `dist`/`.next-verify` and `/api/agent` answered the hot-tub question through
  the server with a guide card. NOT screenshotted: headless Chromium needs `libnspr4`
  (`sudo npx playwright install-deps chromium`) — Joy.
- Server: `/var/www/alexa-plus` cloned, pm2 `alexa-plus` on 3029, DNS `alexa.zettabyteincorp.com`
  live. Joy: the sudo `new-site.sh` line + `SSH_KEY` secret (`docs/ai-memory/deploy.md`).

- `f065dc7` `skill/the-house/` (SKILL.md in the open Agent Skills format + `scripts/house.mjs`, a
  raw-JSON-RPC client proven against the server), `docs/friction-log.md` (7 entries),
  `docs/ai-memory/eligibility.md` (Bangladesh not excluded), `alexa-plus-access.md`.

- `5e7c03a` `f25bd2d` sim as pm2 `alexa-plus-sim` on 3030 (+ workflow copies static/public),
  demo images (wifi QR is a real `WIFI:` payload; hot tub is an SVG illustration — a real photo
  from Joy would be better for the video). Both apps redeployed by hand on the server, verified.

## Morning report

Commits (all on `main`, pushed): `84c62cc` design · `54c1d76` plan · `adbe36b` baseline ·
`105b1bf` MCP Phase 1 · `7f9147c` handoff · `1aa5b57` deploy config · `2c30d1b` deploy state ·
`7eaab71` MCP Phase 2 · `ba1415d` sim · `4c2c548` handoff · `f065dc7` skill + friction log ·
`f44a068` handoff · `5e7c03a` sim deploy · `f25bd2d` images. 14 commits.

Green: `pnpm typecheck`, `pnpm lint`, `pnpm test` = 13 (mcp) + 3 (sim); `pnpm build` both;
`NEXT_DIST_DIR=.next-verify` isolation; MCP Inspector CLI proofs in `docs/proof/`; server pm2
`alexa-plus` (3029) + `alexa-plus-sim` (3030) online, DNS resolving.

**Later the same night:** Joy gave a scoped sudoers entry + set `SSH_KEY`. nginx + TLS live
(`new-site.sh` then hand edits for the static alias and `/mcp`), public checks all green, first CI
deploy `f8e77f6` green. README rewritten `6d953e4`; repo description/homepage/topics set.
**Joy: no submission today — "we worked too fast".** Devpost untouched. No Gemini key: scripted agent.

**Visual pass done** (no WSL libs needed: Playwright Docker image on the server, `~/shots/shot.mjs`,
`--network host`). Found and fixed `7829dc4`: agenda ignored tomorrow (the continuity moment was
silent), "1 coffee pods", ISO dates on cards vs weekdays in speech, guide card repeating the whole
answer, overlapping labels in the hot-tub SVG. Screenshots in `docs/proof/`. **Tagged `phase-1`.**

**Queued for Joy (in order, updated):**
1. Look at https://alexa.zettabyteincorp.com yourself — type the three prompts, listen to the voice.
3. Phase 0 leftovers, when you want: Devpost registration (joythegreatone@gmail.com), forum post
   on Preview access, janet@devpost.com wire question. Not today.
4. Confirm or override: idea #1, name `alexa-plus`, subdomain `alexa`.

Risks: the sim UI has never been seen in a browser by anyone (only its API and static files);
`AGENT=scripted` covers four intents — anything else falls through to the guide lookup; Gemini
model name `gemini-2.5-flash` is a guess at what the free tier serves in 2026-09 — override with
`GEMINI_MODEL`.

**Was next:** Phase 1 deploy — `.github/workflows/deploy.yml` + pm2 ecosystem, following
`~/projects/04_dev/server` (claims pattern; port from `scripts/pm2-ports.sh`). Joy runs the one
`sudo new-site.sh alexa-plus alexa.zettabyteincorp.com <port> --type api` line. Then tag `phase-1`.
After that Phase 2 tools (`get_todays_agenda`, `list_vendors`, `schedule_repair`, `check_supplies`,
`order_supply`, `notify_host`, `host_briefing`) + resources + prompt.

**Now next:** (1) sim deploy — second pm2 app (`alexa-plus-sim`, port 3030) + nginx location on
the same domain, needs Joy's nginx line first; (2) design-critic pass on the sim once Playwright
deps exist; (3) MCP App `property-card`; (4) demo images for `public/img/lakeview/` (hot-tub.jpg,
wifi-qr.png — currently 404, cards render without them); (5) README architecture section.

**Still Joy's:** the three blocks in `docs/ai-memory/deploy.md`; then confirm pick/name/subdomain; Devpost registration
(`joythegreatone@gmail.com`); /rules §3 exclusion list by hand; forum question on Preview access;
janet@devpost.com wire question; Gemini free key (only for the live sim).
