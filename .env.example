# apps/mcp
MCP_TOKEN=change-me          # bearer token every MCP client must present
PORT=3101
PROPERTY_ID=lakeview
DB_PATH=data/house.sqlite
SEED=1                       # seed the demo property on start (idempotent)
